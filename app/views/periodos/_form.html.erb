<%= form_for(periodo) do |f| %>
    <% if periodo.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(periodo.errors.count, "error") %> prohibited this periodo from being saved:</h2>

          <ul>
            <% periodo.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :start %>
      <%= f.date_select :start, :format => :default %>
    </div>

    <div class="field">
      <%= f.label :end %>
      <%= f.date_select :end, :format => :default %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>