<script>
    function toggleHelp() {
        $("#opener").toggleClass("helpActive");
        $(".tooltipHelp").toggleClass("invisible");


        var el = $("#opener");
        if (el.text() == el.data("text-swap")) {
            el.text(el.data("text-original"));
        } else {
            el.data("text-original", el.text());
            el.text(el.data("text-swap"));
        }

    }

</script>

<ul class="nav navbar-right top-nav">
  <li>
    <button onclick="toggleHelp()" data-text-swap="Ocultar ajuda" id="opener" type="button" class="dropbtn">
      Mostrar ajuda
    </button>
  </li>

  <li>
  <a href="<%= new_user_path %>"<i class="fa fa-user-plus" style="color: #51b751"></i> Cadastrar</a>
  </li>
  <li>
  <a href="<%= new_user_sessions_path %>"<i class="fa fa-sign-in" style="color: #74aab7"></i> Login</a>
  </li>

</ul>